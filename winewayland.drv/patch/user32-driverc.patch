--- a/dlls/user32/driver.c	2020-03-28 21:10:34.000000000 +0400
+++ b/dlls/user32/driver.c	2020-03-28 21:10:29.000000000 +0400
@@ -60,6 +60,15 @@
     WCHAR path[MAX_PATH];
     WCHAR key[ARRAY_SIZE(key_pathW) + ARRAY_SIZE(displayW) + 40];
     UINT guid_atom;
+     //TODO add defines
+    ret = LoadLibraryA( "C:\\windows\\system32\\winewayland.drv" ) != NULL;
+    if (!ret) {
+      TRACE( "failed to load 1 %d \n" );
+      ret = LoadLibraryA( "winewayland.drv" ) != NULL;
+    }
+    if( ret ) {
+      return TRUE;
+    }
 
 
     USER_CheckNotLock();
 
